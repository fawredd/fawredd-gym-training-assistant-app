# ARCHIVED — REDIS_CONFIG.md

(Archived on 2026-04-03 — moved from active artifacts. Kept for reference; not part of active implementation docs.)

---

```text
<original REDIS_CONFIG.md content archived>
```

If Redis config becomes part of the active infra plan, restore and reconcile with `docker-compose.yml` and CI secrets.
